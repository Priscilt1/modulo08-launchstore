{% extends 'layout.njk' %}
{% import 'parts/card.njk' as card %}

{% block content %}

    {% call card.render('Cadastrar Produto', 'product-create container')%}

        {# enctype="multipart/form-data" permite receber - arquivos upload de imagem (precisa instalar o node) #}
        <form action="/products" method="post" enctype="multipart/form-data">
        
            <div class="fields">
                {% include 'products/fields.njk'%}
            </div>

            <button class="button" type="submit" onclick="Validate.allFields(event)"> Salvar </button>

        </form>
    {% endcall %}


{% endblock content %}